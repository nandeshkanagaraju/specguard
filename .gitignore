# The demo fixture is its own git repository with `clean` and `drifted` tags.
# It cannot be nested inside this one, so it is built after clone by
# scripts/build_fixture.sh (or .ps1) from samples/_variants/.
samples/orderflow/.git/

.specguard/
.venv/
venv/
dist/
build/
*.egg-info/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.DS_Store
.idea/
*.swp
